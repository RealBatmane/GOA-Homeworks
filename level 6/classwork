; 1) კომენტარის სახით ახსენი, რა არის snake_case, camelCase და PascalCase.

; 2) კომენტარებით ახსენი, რა არის input და რა არის output. თითოეულზე მოიყვანე 2-2 მაგალითი.

; 3) მომხმარებელს შეაყვანინე შენი ასაკი და შექმენი ცვლადი snake_case-ით. შედეგად გამოიტანე თქვენს ასაკზე 1-დან 10-მდე ნებისმიერი რიცხვით გამოკლებული მნიშვნელობა.

; 4) შექმენი ცვლადი სახელად my_name და მომხმარებელს შეაყვანინე შენი სახელი.
; შექმენი მეორე ცვლადი სახელად user_name და მომხმარებელს შეაყვანინე შენი გვარი.
; საბოლოოდ, მოახდინე ამ ორ ცვლადზე კონკატინაცია.


; snake_case rodesac sityvas _ aqvs radgan kargad wavikitxot da space ar gamoviyenot rac errors gamoiwvevs
; camelCase meore winadadebis pirveli aso rodesac didia magalitad doingHomework
; PascalCase rodesac pirveli da meore sityvis pirveli aso didia YearsOld


; input aris rodesac vkrifav texts da misdis kompiuters
; output aris rodesac monitori gvachvenebs da agiqvavs gamosaxulebas an kide yursasmenebshi ro xma shemodis


user_age = input("Enter user age: ")
print(int(user_age) - 5)

user_name = input("Enter user name: ")
user_surname = input("Enter user surname: ")

print(user_name,user_surname)